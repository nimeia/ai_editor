# AI Bridge VSCode Extension MVP

This extension is a minimal VSCode surface for the controlled editing session flow:

- search workspace text
- stage line-range replacements or replace-from-search-result
- inspect staged files and change blocks
- preview diffs with risk summaries
- reject a staged change or a whole staged file
- commit, abort, recover, and rebase sessions
