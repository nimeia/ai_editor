# V1 最终发布检查清单

## 1. 源码与文档

- [ ] 版本号确认无误
- [ ] `README.md` 与当前实现一致
- [ ] `docs/04-V1-协议草案.md` 与当前方法/错误码一致
- [ ] `docs/07-V1-构建运行与测试指南.md` 可直接指导构建与验证
- [ ] `docs/08-V1-发布与部署指南.md` 可直接指导打包与部署
- [ ] `docs/09-V1-验证报告.md` 已更新为本次候选版本的实际结果

## 2. Linux / POSIX 验证

- [ ] `cmake -S . -B build -DCMAKE_BUILD_TYPE=Release`
- [ ] `cmake --build build --parallel 1` 或其他确认稳定的并行度
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] `./scripts/validate_v1.sh --build-dir build --jobs 1`
- [ ] 手工查看 `.bridge/audit.log` 与 `.bridge/history.log`

## 3. Windows 验证

- [ ] `cmake -S . -B build -G "Visual Studio 17 2022" -A x64`
- [ ] `cmake --build build --config Release --parallel 1` 或其他确认稳定的并行度
- [ ] `ctest --test-dir build -C Release --output-on-failure`
- [ ] `pwsh ./scripts/windows_smoke.ps1 -BuildDir ./build -Config Release`
- [ ] `pwsh ./scripts/validate_v1.ps1 -BuildDir build -Config Release -Jobs 1`
- [ ] 检查 `.p6_validation_summary/windows_validation_summary.json` 中所有阶段 `ok=true`
- [ ] 检查 `.p6_validation_summary/windows_validation_summary.md` 中 install checks / artifacts 摘要

## 4. 安装与归档

- [ ] `cmake --install build --prefix install-root`
- [ ] 安装树内存在 `bin/bridge_daemon` 与 `bin/bridge_cli`
- [ ] 安装树内存在 `share/ai_bridge/docs/*`
- [ ] POSIX 归档：`./scripts/package_release.sh --build-dir build --out-dir dist --generator TGZ --jobs 1`
- [ ] Windows 归档：`pwsh ./scripts/package_release.ps1 -BuildDir build -Config Release -OutDir dist -Generator ZIP -Jobs 1`
- [ ] `SHA256SUMS.txt` 已生成并校验

## 5. 核心链路抽查

- [ ] `ping`
- [ ] `info`
- [ ] `open`
- [ ] `list`
- [ ] `stat`
- [ ] `read`
- [ ] `read-range`
- [ ] `search-text`
- [ ] `search-regex`
- [ ] `read --stream`
- [ ] `patch-preview`
- [ ] `patch-apply`
- [ ] `history`
- [ ] `patch-rollback`
- [ ] `cancel`
- [ ] `timeout`

## 6. 发布决策

- [ ] 本次候选版本没有阻塞级 crash / 数据损坏问题
- [ ] release artifact 已归档保存
- [ ] 哈希文件已归档保存
- [ ] 已记录 Windows 与 Linux 的最终验证结果
- [ ] 允许进入 V1 发布
