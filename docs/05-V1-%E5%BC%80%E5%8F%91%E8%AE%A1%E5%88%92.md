# V1 开发计划

## 已完成里程碑

### M1 基础骨架与通信

- 仓库初始化
- CMake 分层
- Core / Platform / Transport 模块拆分
- 本地 IPC 握手
- `daemon.ping`
- `workspace.info`

### M2 Workspace + PathPolicy

- `workspace.open`
- instance key 生成与隔离
- 路径归一化
- workspace containment
- `deny / skip_by_default / normal`
- 默认软排除目录
- 单元测试 + 集成测试

### M3 FileService

- `fs.list`
- `fs.stat`
- `fs.read`
- `fs.read_range`
- 二进制判定、编码、BOM、EOL、line count 元信息

### M4 SearchService

- `search.text`
- `search.regex`
- include/exclude
- 结果上限
- timeout / cancellation check

### M5 PatchService

- `patch.preview`
- `patch.apply`
- `patch.rollback`
- `history.list`
- preview/apply transaction baseline via `preview_id`
- preview lifecycle states
- richer conflict diagnostics
- rollback restored hash / mtime metadata

### M6 日志、错误码与工程化基础

- `runtime.log`
- `audit.log`
- history log
- 错误码标准化
- runtime / audit / history rotation
- backup / preview cleanup baseline
- CLI `--json` / 人类可读双输出

### M7 Windows 原生收口

- POSIX / Windows 平台与传输层源码分拆
- Windows Named Pipe baseline
- GitHub Actions Linux + Windows CI
- Windows native PowerShell integration coverage in CTest
- Windows unicode / space-path coverage
- smoke reuse of existing build output + daemon log dump on failure
- `workspace.info` / `workspace.open` runtime diagnostics fields

### M8 协议一致性增强

- search / file read / patch preview 流式响应基线
- stream final summary metadata 统一
- timeout 扩展到 streaming file read / patch preview
- request.cancel 扩展到 streaming file read / patch preview

## 当前状态

当前仓库已经达到：

- 核心能力闭环
- Windows 原生链路可验证
- 协议、日志、patch 生命周期已形成 V1 基线
- 主要剩余工作转向发布工程化与最终验证

## 下一阶段

### P5 发布工程化基线

- 版本号 / 产物命名约定
- packaging / release script baseline
- 部署 / runtime 目录说明
- 最小运行手册按发布产物口径补齐

### P6 全量验证与收尾

- Linux 全量回归
- Windows 全量回归
- 单元测试 / 集成测试全跑
- 手工核心链路验证
- V1 验证报告
- final release checklist
